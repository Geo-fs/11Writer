# Phase 2 Next Biggest Wins Packet

This packet gives Manager AI one compact planning surface for the next three high-value controlled-agent handoffs per lane without reopening long progress threads or older source-batch docs.

Use it to answer:

- what the next three biggest safe wins are per controlled agent
- whether the next step is a source build, workflow hardening pass, consumer follow-on, or governance sweep
- what caveats must survive the handoff
- which items are blocked by validation risk versus implementation risk

Status rule:

- [source-assignment-board.md](/C:/Users/mike/11Writer/app/docs/source-assignment-board.md) remains the source-status truth.
- [source-validation-status.md](/C:/Users/mike/11Writer/app/docs/source-validation-status.md) remains the validation-traceability truth.
- Atlas and Wonder remain peer or user-directed input only.
- Source Discovery runtime, Wave LLM runtime, Browser Use guidance, Atlas Source Discovery Ten-Step notes, and Wonder macOS/plugin/connector planning are planning and runtime-boundary input here, not source-validation proof.

## Global Rules

- Keep `implemented`, `contract-tested`, `workflow-validated`, and `fully validated` distinct.
- Do not treat helper packages, review queues, runtime status endpoints, scheduler compatibility helpers, Browser Use guidance, or peer research as source-validation proof.
- Keep source-health, evidence basis, caveats, and no-harmful-use boundaries explicit in every handoff.
- Prefer bounded follow-on work over opening multiple fresh lanes when one lane is already partially implemented.

## Feature Occupancy Notes

- `Evidence Timeline` is partially occupied already through Aerospace's implemented evidence-timeline package and Atlas's peer/user-directed analyst-workbench timeline surface; future work should target cross-domain normalization only.
- `Review Packet Export` is partially occupied already through Geospatial Canada export packages, Marine report/export helpers, Aerospace review/export packages, and Features/Webcam source-ops export surfaces; future work should unify contract/caveat structure rather than add renamed per-domain packet helpers.
- `Source Candidate Intake Queue` is partially occupied already through Gather routing/governance docs, Features/Webcam candidate review-priority surfaces, and Source Discovery/Atlas/Wonder candidate-routing input; future work should be a cross-lane governance contract only.
- `Source Health Console` is partially occupied already through Geospatial family review/export, Marine source-health workflows, Aerospace source-readiness/context-availability packages, and Data AI methodology-aware source-health summaries; future work should aggregate those domain surfaces rather than rebuild them.
- `Attention Queue Unifier` is partially occupied already through Marine attention queues, Aerospace context review queues, Geospatial environmental review queues, Features/Webcam review-priority surfaces, and Data AI family/source review metadata; future work should normalize shared contracts rather than create another queue concept.
- `Source Fusion Snapshot` remains open globally, but it should compose the existing domain packages instead of replacing them or treating the feature as blank.

## Connect AI

| Rank | Source or workflow id | First safe slice | Why it matters now | Source or evidence caveats | Validation or status risk | Handoff type | Do-not-do boundary |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `post-wave-compatibility-and-peer-runtime-classification-sweep` | keep the completed reporting-loop package contract stable and align peer/runtime classifications after the current wave | The contract and regression work are complete, so the next Connect value is governance truth and compatibility stability rather than another fresh contract wave. | Peer/runtime slices remain below implementation or validation proof. | `medium` because status drift can silently over-promote peer surfaces. | `governance rollout` | Do not convert peer/runtime alignment into source-validation proof. |
| 2 | `repo-wide-validation-checkpoint` | rerun targeted compile, lint, build, smoke, alerts, ownership-scan, and validation snapshot after the new wave stabilizes | This is still the fastest way to keep status docs honest after multiple agent completions. | Validation truth must stay machine-specific where Browser or Playwright launch is host-bound. | `medium` because green checkpoints can still hide mixed-worktree collision risk. | `validation sweep` | Do not convert one host's green run into universal workflow validation. |
| 3 | `peer-runtime-warning-reduction` | reduce noisy peer/runtime warning classes without changing meaning | Atlas and Wonder peer/runtime surfaces are now materially useful, so warning output must stay readable without widening semantics. | Source Discovery, media geolocation, Statuspage, and Mastodon surfaces remain review or runtime infrastructure only. | `medium` because cleanup can drift into semantic changes if not bounded. | `runtime hardening` | Do not imply hidden polling, automatic approval, or live scheduler proof. |

## Data AI

| Rank | Source or workflow id | First safe slice | Why it matters now | Source or evidence caveats | Validation or status risk | Handoff type | Do-not-do boundary |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `data-topic-scoped-report-packet` | build one bounded topic-scoped report packet over the already implemented family stack | World-news awareness is complete, so the next larger Data move is composition over existing families rather than another fresh family. | Feed text remains contextual only and untrusted. | `high` because grouped report composition can overcollapse evidence classes. | `workflow follow-on` | Do not turn contextual feed text into primary event truth, attribution proof, or action guidance. |
| 2 | `data-review-export-coherence` | keep family review, review-queue, readiness, source intelligence, fusion snapshot, and report-brief surfaces aligned across the already implemented grouped feed waves | Multiple grouped waves and helper layers now exist, so the next useful follow-through is coherence rather than more helper proliferation. | Review metadata remains helper-only and must not become source scoring or validation proof. | `high` because helper surfaces can be mistaken for trust or action guidance. | `workflow follow-on` | Do not convert review/export helpers into credibility, severity, attribution, or action engines. |
| 3 | `one-bounded-data-topic-coverage-closure` | reopen one small topic-coverage or workflow-closure item only after the topic-scoped packet stays stable | Fresh Data work should remain small and deliberate after the world-news wave. | Feed text remains untrusted data and contextual only. | `medium` because grouped overlap is easy to overread. | `workflow follow-on` | Do not fetch linked pages, scrape interactive sites, or add scoring/attribution semantics. |

## Geospatial AI

| Rank | Source or workflow id | First safe slice | Why it matters now | Source or evidence caveats | Validation or status risk | Handoff type | Do-not-do boundary |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `belgium-rmi-warnings-plus-reporting-integration` | verify and implement one bounded Belgium RMI warning feed or machine-readable warning slice, then thread it into the completed environmental reporting inputs | DWD is complete, so the next geospatial wave should stay warning-focused and bounded. | Warning semantics remain advisory/contextual only and may still need endpoint-verification discipline. | `high` because verification and advisory integration can both sprawl. | `source build plus workflow follow-on` | Do not widen from one warning slice into a broad multi-product or HTML-first scrape. |
| 2 | `environmental-report-input-follow-through` | build one bounded question-driven report-input/export path over already implemented environmental hazard, weather, and reference slices | Reporting-desk value now comes more from composing existing geospatial inputs than from reopening already built sources. | Observed, advisory, modeled, and static-reference semantics must stay separate. | `high` because cross-source composition can drift into overclaiming if caveats collapse. | `workflow follow-on` | Do not turn fused environmental context into intent, impact, or action conclusions. |
| 3 | `one-fresh-geospatial-warning-follow-on` | stage one post-Belgium European warning follow-on only after the current wave is stable | The next geospatial reopening after Belgium RMI should stay narrow and warning-focused. | Follow-ons such as Meteoalarm must still preserve advisory-only semantics and verification gates. | `medium` because European warning families can sprawl quickly. | `source build` | Do not widen from one warning family into a multi-product family sweep. |

## Marine AI

| Rank | Source or workflow id | First safe slice | Why it matters now | Source or evidence caveats | Validation or status risk | Handoff type | Do-not-do boundary |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `marine-corridor-situation-report-package` | build one bounded corridor situation/report package on top of the completed marine fusion and report-brief stack | The report-brief package is complete, so the next marine move should stay on bounded situation reporting instead of a fresh source. | Marine review/export packages remain contextual only and must not imply intent, impact, or action need. | `high` because corridor summaries are easy to over-promote. | `workflow follow-on` | Do not turn marine reporting helpers into crisis, wrongdoing, or operational claims. |
| 2 | `netherlands-rws-waterinfo-validation-follow-through` | keep the implemented WaterWebservices slice plus the completed helper/export follow-on bounded and record only a narrow validation/export note | The raw slice and helper/export follow-ons already exist, so the next value is validation coherence rather than another raw source or helper build. | Waterinfo is approved only for the narrow POST-based slice. | `medium` because widening into portal or historical families would break the approval basis. | `workflow follow-on` | Do not use viewer pages, broaden into portal ingestion, or infer impact from water-level values alone. |
| 3 | `marine-source-row-workflow-closure` | keep source-row evidence and workflow notes aligned under the new corridor/report package | Reporting-safe marine closure still depends on careful row-level caveat and evidence-basis preservation. | Source-row evidence remains narrower than global marine truth. | `medium` because row-level status can drift upward if phrasing is loose. | `workflow follow-on` | Do not collapse report helper evidence into external-source validation proof. |

## Aerospace AI

| Rank | Source or workflow id | First safe slice | Why it matters now | Source or evidence caveats | Validation or status risk | Handoff type | Do-not-do boundary |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `aerospace-space-weather-continuity-package` | build one bounded space-weather continuity package on top of the completed aerospace reporting stack | VAAC report follow-through is complete, so the next aerospace move should stay bounded and continuity-focused rather than reopen another source. | Observed, archive, forecast, and continuity semantics must stay separate. | `high` because continuity packaging can be mistaken for operational consequence. | `workflow follow-on` | Do not infer route impact, threat, or action from continuity packaging. |
| 2 | `aerospace-smoke-rerun-path` | rerun aerospace smoke only on a Windows host where Playwright can launch | The main remaining workflow-validation gap is still host-capable smoke, not missing source coverage. | Prepared smoke is not executed smoke, and Windows `spawn EPERM` is a host caveat. | `high` because current host limits can be misread as workflow success or failure. | `workflow hardening` | Do not reopen fresh aerospace source work just to avoid the validation gap. |
| 3 | `aviationWeatherContext-workflow` | rerun AWC, FAA NAS, CNEOS, SWPC, OpenSky, and continuity-package smoke/export checks once browser launch is available | Aerospace already has the deepest contract-tested source stack, so the highest value is workflow closure after the host blocker is resolved. | Observed, forecast, archive, and reference contexts must stay separate. | `high` because browser-smoke absence is the last major gap. | `workflow hardening` | Do not reopen fresh aerospace source work just to avoid the validation gap. |

## Features/Webcam AI

| Rank | Source or workflow id | First safe slice | Why it matters now | Source or evidence caveats | Validation or status risk | Handoff type | Do-not-do boundary |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `nzta-and-arlington-sandbox-feasibility-plus-bounded-candidate-growth` | take NZTA and Arlington through bounded feasibility review only and keep one narrow candidate follow-on | Caltrans is now complete as a sandbox-importable candidate-only pass, so the next camera move should stay on the next two bounded endpoint candidates. | Candidate and endpoint-verified states remain pre-implementation only. | `high` because feasibility notes are easy to over-promote into activation or validation proof. | `workflow follow-on` | Do not activate, schedule, validate, or imply live ingest for endpoint-verified or candidate-only camera sources. |
| 2 | `camera-post-sandbox-comparison-follow-on` | compare sandbox burden, source-health expectation, and media posture across the current candidate set | This is the cleanest next source-ops follow-through after the new feasibility wave. | Candidate and endpoint-verified states remain pre-implementation only. | `high` because source-ops summaries are easy to over-promote into validation proof. | `workflow follow-on` | Do not activate, schedule, validate, or imply live ingest for candidate-only camera sources. |
| 3 | `finland-digitraffic` | one bounded consumer or status-classification follow-on over the implemented backend slice | This remains the cleanest next operational-context win without opening another large camera lane. | Keep road-weather scope separate from cameras or transport analytics sprawl. | `medium` because there is still no workflow validation record. | `consumer follow-on` | Do not widen into full transport analytics or cross-mode feed fusion. |

## Gather AI

| Rank | Source or workflow id | First safe slice | Why it matters now | Source or evidence caveats | Validation or status risk | Handoff type | Do-not-do boundary |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `status-reconciliation-after-each-wave` | keep board, validation, workflow, prompt, and routing docs synchronized after each controlled-agent completion | Multiple lanes are moving faster than the docs, so stale status is now one of the main failure modes. | Gather owns truth reconciliation, not implementation proof. | `high` because mixed status surfaces can misroute whole waves. | `governance` | Do not promote helper or peer evidence into workflow validation. |
| 2 | `held-source-verification-queue` | verify one bounded held source at a time, starting from the assignment board and packet docs | This is the cleanest way to keep the next source wave moving without opening vague prompts. | Held verification is not implementation or approval by itself. | `medium` because endpoint pinning can be mistaken for broader product-family approval. | `verification memo` | Do not widen a narrow verification memo into a broad source-family approval. |
| 3 | `peer-input-governance` | keep Atlas, Wonder, Browser Use guidance, and Source Discovery runtime work represented as peer/runtime input only | Peer contributions are now materially useful, but they are easy to over-promote. | Peer and user-directed work is not Manager-owned validation proof. | `high` because governance drift can reclassify peer input as implementation truth. | `governance` | Do not convert peer docs, audits, or runtime notes into source-validation proof. |

## Immediate Manager Order

If Manager AI wants the fastest next larger wins without reopening prompt archaeology:

1. `Connect`: `post-wave-compatibility-and-peer-runtime-classification-sweep`
2. `Data`: `data-topic-scoped-report-packet`
3. `Marine`: `marine-corridor-situation-report-package`
4. `Aerospace`: `aerospace-space-weather-continuity-package`
5. `Geospatial`: `belgium-rmi-warnings-plus-reporting-integration`
6. `Features/Webcam`: `nzta-and-arlington-sandbox-feasibility-plus-bounded-candidate-growth`
7. `Gather`: `status-reconciliation-after-each-wave`

## Peer Input Note

- Atlas Source Discovery runtime review remains peer/runtime input only unless Connect separately validates the current runtime boundaries, and the newer Atlas runtime operator-console slice should stay in that same pending-validation posture.
- Wonder Browser Use and browser-security guidance remain verification guidance only and should inform agent behavior, not source-validation promotion.
- Wonder macOS/plugin/connector planning remains peer planning input only and should inform routing boundaries, not implementation or source-validation promotion.
